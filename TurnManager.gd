extends Node2D

onready var game = get_tree().current_scene

# TODO: the purpose of this Node is to manage turns, i.e.
# the actions of each active card on each cycle,
# the actions of players on each turn (when they can play cards)
# https://github.com/Multi-User-Domain/games-transformed-jam-2023/issues/4

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
